<!-- Navbar -->

<nav
        class="layout-navbar container-xxl navbar navbar-expand-xl navbar-detached align-items-center bg-navbar-theme"
        id="layout-navbar">
    <div class="layout-menu-toggle navbar-nav align-items-xl-center me-3 me-xl-0 d-xl-none">
        <a class="nav-item nav-link px-0 me-xl-4" href="javascript:void(0)">
            <i class="bx bx-menu bx-sm"></i>
        </a>
    </div>

    <div class="navbar-nav-right d-flex align-items-center" id="navbar-collapse">
        <!-- Search -->
        <div class="navbar-nav align-items-center">
            <div class="nav-item d-flex align-items-center"> 
                <span class="fw-semibold">Xin chào, <%= user.full_name %></span>
            </div>
        </div>

        <!-- /Search -->

        <ul class="navbar-nav flex-row align-items-center ms-auto">
            <li class="nav-item lh-1 me-3">

            </li>

            <!-- User -->
            <li class="nav-item navbar-dropdown dropdown-user dropdown">
                <a class="nav-link dropdown-toggle hide-arrow" href="javascript:void(0);"
                   data-bs-toggle="dropdown">
                    <div class="avatar avatar-online">
                        <% if(user.role == 'ADMIN') { %>
                            <img src="/assets/avatars/profile.png" alt class="w-px-40 h-auto rounded-circle"/>
                        <% } else { %>
                            <img src="/assets/avatars/profile_user.png" alt class="w-px-40 h-auto rounded-circle"/>
                        <% } %>

                    </div>

                </a>
                <ul class="dropdown-menu dropdown-menu-end">

                    <li>
                        <a class="dropdown-item" href="profile">
                            <div class="d-flex">
                                <div class="flex-shrink-0 me-3">
                                    <div class="avatar avatar-online">
                                        
                                        <% if(user.role == 'ADMIN') { %>
                                            <img src="/assets/avatars/profile.png" alt
                                        class="w-px-40 h-auto rounded-circle"/>
                                        <% } else { %>
                                            <img src="/assets/avatars/profile_user.png" alt
                                            class="w-px-40 h-auto rounded-circle"/>
                                        <% } %>
                                    </div>
                                </div>
                                <div class="flex-grow-1">
                                    <span class="fw-semibold d-block"><%= user.full_name %></span>
                                    <% if(user.status == 2) { %>
                                        <small class="text-muted">
                                            <span class="badge bg-success">Đã xác minh</span>
                                        </small>
                                    <% } else if(user.status == 3) { %>
                                        <small class="text-muted">
                                            <span class="badge bg-danger">Chờ cập nhật</span>
                                        </small>
                                    <% } else  { %>
                                        <small class="text-muted">
                                            <span class="badge bg-danger">Chờ xác minh</span>
                                        </small>
                                    <% } %>

                                </div>
                            </div>
                        </a>
                    </li>
                    <li>
                        <div class="dropdown-divider"></div>
                    </li>
                    <li>
                        <a class="dropdown-item" href="/profile">
                            <i class="bx bx-user me-2"></i>
                            <span class="align-middle">My Profile</span>
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href="/change-password">
                            <i class="bx bx-cog me-2"></i>
                            <span class="align-middle">Thay đổi mật khẩu</span>
                        </a>
                    </li>

                    <li>
                        <div class="dropdown-divider"></div>
                    </li>
                    <li>
                        <a class="dropdown-item" href="/logout">
                            <i class="bx bx-power-off me-2"></i>
                            <span class="align-middle">Đăng xuất</span>
                        </a>
                    </li>
                </ul>
            </li>
            <!--/ User -->
        </ul>
    </div>
</nav>

<!-- / Navbar -->
